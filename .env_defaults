# Copy this file to .env for local development. Never commit the real key.
# Use the exact same randomly generated value in the SRS_QUT environment.
SRS_API_SHARED_KEY=

# Existing deployment settings are shown here only as safe examples.
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
